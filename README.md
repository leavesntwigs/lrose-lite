# lrose-lite
Functional form of lrose applications for interoperability with Open Radar Science packages

lrose-lite will probably be a shared library of lrose applications with ctype python bindings.
All I/O is done externally in the calling environment.  Data are passed as base python data types and numpy arrays.
